# 名著改写：独立分级改编流程设计

日期：2026-09-13  
状态：MVP 已实施（2026-09-13）。本文定义的新流程不修改现有原创生成行为。

当前实现范围：已接通独立 `classic-v1` 管线、专用断点、目录/API/CLI/页面分流、版本固定与全章幂等发布；首个已核对资产为 `Aesop's Fables · The Lion and the Mouse`，支持 Starter 与 Stage 1 的两章组合。新增作品只通过审核后的资产目录开放。

## 1. 产品定位与命名

产品提供两类故事：**原创**、**名著改写**。名著改写替换现有“名著仿写”的生成方式，页面统一使用“名著改写”，说明文案为“依据原作，为你的英语阅读水平改编”。不再把它描述为模仿作品风格的原创。

本设计将需求中的“增加一种故事类型”解释为补齐名著改写的独立产品入口和生成能力；现有名著仿写迁入该类型，不再并列增加一个含义重叠的第三入口。

| 产品类型 | 内部选材模式 | 处理方式 |
| --- | --- | --- |
| 原创 | `original` | 保留当前生成、评审、修稿和发布流程 |
| 原创中的兴趣参照 | `favorite` | 保留现有行为，仍生成独立原创故事，不进入忠实改编流程 |
| 名著改写（原名著仿写） | `classic` | 使用本文的新流程 |

继续使用 `classic`，不新增 `imitating` 枚举，不因页面改名破坏现有 CLI 参数。

核心流程：

```text
内容准备，一份原作范围只做一次：
原作文本 → 故事底稿 → 对照原文核对 → 发布可复用底稿

用户请求，按目标等级执行：
已核对底稿＋原作范围 → 整篇分级改编 → 一次整体编辑检查
                                          ↓
                                   定稿并保存正文
                                          ↓
                                添加学习词和阅读题
                                          ↓
                                  入库、逐章解锁
```

“核对过”必须有实际核对记录；模型自己输出 `verified: true` 不构成核对。

### 1.1 项目写作依据

名著改写必须引用以下两份项目文档，作为分级改编与一次整体编辑的共同输入依据：

- [story-bible.md](/Users/zhuangzhineng/Documents/ai_workspace/read-remember/story-bible.md)：好故事的语言、结构、内容原则，以及故事模式和分集技巧。
- [good-story-demo.md](/Users/zhuangzhineng/Documents/ai_workspace/read-remember/good-story-demo.md)：《The Broken Robot》《The Shadow Map》两份单篇示例，以及从《The Strange Signal》到《The Final Face-Off》的五集故事样例。单篇示例展示行动因果，分集样例展示章节承接、局部推进与整篇收束。

这里的 `story-bible.md` 是项目写作指导，不是旧流水线由模型生成的某个系列的 `storyBible` 状态对象；引用它不会重新引入故事圣经生成、线索账本或多轮评分。

原作提供“发生了什么”，核对底稿确定“保留什么”，目标档位决定“怎样表达”，两份参考文档帮助“讲得清楚且吸引人”。范例不提供改编故事的事实，也不预设为 Starter 合格词汇样本。

## 2. 现状与要解决的问题

当前代码的实际情况：

- `server/scripts/story-generation/catalog.ts` 已定义 `original / classic / favorite`，名著目录只有标题、作者、`storyCore`、`childAppeal`，没有可追溯的原文和核对底稿。
- `pipeline.ts` 的 `sourceBrief()` 给 `classic` 添加名著改写提示，但仍进入通用生成流水线。仅凭简介要求“忠实原作”，不能确保模型使用的事件真的来自原作。
- `server/src/custom-story.ts` 的用户定制生成入口目前固定使用 `sourceMode: "favorite"`（产品语义仍属原创兴趣故事）；前端类型、请求参数、数据库和队列分发都需要接入名著选择信息，不能只增加一个按钮。
- 现有流水线有多候选、融合、季纲调整、连续性状态、线索账本、评分与重试。这些属于原创现有行为，名著改写不继承。

新流程减少三类不确定性：不再现场发明主线；不再让多份模型摘要竞争成为事实依据；不再用多轮浮点评分决定是否反复重写。

质量目标是忠实、讲得通、适龄且易读。减少调用是设计手段，不预先承诺成功率或响应秒数。

## 3. 选材与原文资产

### 3.1 第一版范围

只开放少量已经完成原文准备与底稿核对的作品单元。优先使用短篇、寓言，或长篇里有明确目标、发展和局部结局的独立片段。

不把整部长篇硬压成三章短文。选择长篇片段时，展示“某作品·某片段”，标明改编范围；片段结束不冒充整部原作结局。

选材标准：主要人物少、事件因果可观察、背景知识负担小、结尾明确、情节适合目标读者年龄。语言等级不等于读者年龄。

第一版不开放用户仅输入任意书名后立即改编，也不在用户等待期间抓取全书。未准备的作品显示暂不可选。

### 3.2 原文来源

每个单元保存实际原文，指定版本、语言、来源、篇章范围及文本哈希。中文原作可以直接依据中文原文改编成分级英语，无须增加一个全文翻译中间阶段。

采用可用于该发布场景的公版或已授权文本。具体版本、译本单独记录来源与使用依据，不能只因原作者年代久远就把现代译本或商业分级读物当作源文本。

最小资产目录建议：

```text
server/data/classics/<workId>/<unitId>/
  source.txt       # 固定版本原文，段落带稳定编号
  manifest.json    # 来源、范围、版本、适用等级和已批准阅读长度
  base.json        # 人工核对并发布的故事底稿
```

原文较大或不宜进入 Git 时，使用服务端持久化文件目录并备份；目录清单和版本规则仍需可追踪。第一版不引入向量数据库、检索代理或自动采集系统。

建议的最小来源字段：

| 字段 | 用途 |
| --- | --- |
| `workId / unitId` | 区分作品和改编片段，不能只用一个书名标识 |
| `title / author / language` | 来源展示 |
| `edition / sourceUrl / scope` | 明确依据哪个版本的哪些内容 |
| `usageBasis` | 记录该源文本的使用依据 |
| `sourceHash / sourceVersion` | 固定输入，原文变更时失效旧核对状态 |
| `supportedProfiles` | 已批准的等级、篇幅范围和章数组合 |

已有 `classicId` 可保留为作品标识；新增 `unitId`，解决《伊索寓言》、福尔摩斯故事集以及长篇多个片段无法仅靠 `classicId` 区分的问题。

## 4. 核对过的故事底稿

### 4.1 底稿内容

底稿是简短、可读的内容资产，不是另一套巨型策划 Schema：

```json
{
  "characters": [
    { "name": "原作人物固定称呼", "role": "主要关系和动机" }
  ],
  "context": "读懂本片段必需的背景",
  "events": [
    {
      "id": "E1",
      "what": "实际发生的关键事件",
      "why": "原作明确提供的原因；未说明则写未说明",
      "result": "事件带来的结果",
      "sourceParagraphs": ["p001", "p003"]
    }
  ],
  "ending": "本范围的结局与情感落点",
  "mustKeep": ["不可删去的动机、转折、伏笔或结果"],
  "canOmit": ["可以省略的支线、背景或描写"]
}
```

来源和核对信息由程序存放在资产记录中：`baseVersion`、`sourceHash`、`status`、`checkedBy`、`checkedAt`。事件编号帮助编辑对照原作，不要求生成正文填写逐段事件证据表。

准备底稿时，参照 [story-bible.md](/Users/zhuangzhineng/Documents/ai_workspace/read-remember/story-bible.md) 的故事模式，辨认原作已有的推动力：目标与选择、倒计时、解谜、反转或首尾呼应。底稿应保留支撑该推动力的关键事件与情感落点，不为套用某一种模式新增原作没有的失败、危机或领悟，也不要求模型新增模式识别字段。

### 4.2 核对方式

1. 编辑选定完整原文范围并建立稳定段落编号。
2. 可以由模型根据实际原文生成一份底稿草案，也可以人工直接编写。
3. 程序检查引用段落存在，必要字段完整。
4. 内容编辑对照原文检查人物、动机、事件顺序、因果和结局，修正遗漏或幻觉后发布为 `verified`。
5. 只有 `verified` 且 `sourceHash` 匹配的底稿可以用于用户请求。

程序只能验证引用位置和字段，不能证明内容忠实；第一版的人工核对是本设计明确选择的质量投入，发生在内容入库时，不阻塞每个用户请求。

更新原作范围或底稿时创建新版本，不覆盖已发布故事所绑定的旧版本。旧阅读记录继续指向旧成品。

## 5. 分级改编

### 5.1 输入与事实优先级

每次改编提供：选定范围的实际原文、已核对底稿、目标阅读档位、目标读者年龄背景、章数及总篇幅，以及从 `story-bible.md` 选取的适用原则、从 `good-story-demo.md` 选取的一份范例及其使用说明。不能只把两个文件名写进提示，假定模型能自行读取本地文件。

事实依据为指定原文；底稿是经过核对的内容选择与压缩说明。发现两者实质矛盾时标记资产问题并停止，不让模型临时决定新的故事事实。

在满足原作事实的前提下，目标等级与本文改编边界优先于通用技巧和范例写法。例如原作没有反转就不强加反转，Starter 不为模仿强动词而使用难以理解的词。无法同时满足忠实性和阅读负荷时回到选材范围处理，不改造原作真相。

不同等级直接从同一原文和底稿生成，不采用“高级版 → 中级版 → 初级版”的逐层压缩。

### 5.1.1 两份参考文档如何进入提示

| 来源 | 提取与采用 | 名著改写中的边界 |
| --- | --- | --- |
| `story-bible.md`：语言层 | 常用词、具体名词和动作、主动语态、短而有节奏的句子 | 表意准确优先；`crept` 等示例不是必须使用的词，低档位可采用清楚的常用短语 |
| `story-bible.md`：结构层 | 简短自然段、清楚的目标与因果、关键选择有可见结果 | 每段 2–4 句作为排版倾向，不作失败条件；不强制所有原作都套同一种结构 |
| `story-bible.md`：内容层 | 从相关动作或冲突进入，用行为表达情感，保留原作结尾的回报 | 允许为初学者直接解释动机，不因使用 “He was scared” 一类明示句扣分 |
| `story-bible.md`：分集技巧 | 在原作已有转折处分章，用一句必要承接唤醒记忆 | 终章收束；不强制“未完待续”、危机逐章升级或多视角轮换，不把必要承接判为重复 |
| `good-story-demo.md`：The Broken Robot | 学习“原计划受阻 → 改变策略 → 行动改变结果”的清晰因果 | 不复制机器人比赛、角色、台词或结局；只有原作有对应行动时才使用该方法 |
| `good-story-demo.md`：The Shadow Map | 学习“情绪压力 → 主动选择 → 利用已知信息行动 → 情感回报” | 不移植丢地图、影子导航或自然知识推理；示例中的复杂表达不得当作目标档位标准 |
| `good-story-demo.md`：分集故事样例（Part 1–5） | 学习前集结果如何成为后集起点，每集完成具体行动、留下自然期待，终集回应中心问题 | 不复制智能家居、通风管、倒计时或演习反转；不强制五集，不要求原作逐章升级危险，也不将样例视为事实逻辑和分级语言均已核验的成品 |

第一版由内容编辑为单元指定范例，不增加模型选例请求。单篇写法默认使用《The Broken Robot》，也可选择《The Shadow Map》；多章改编默认采用分集故事样例的章节功能摘要与一组实际相邻章节接缝，不再默认附单篇全文。编辑可以另选更适合原作的示例，但多章任务仍须包含下文的承接原则。

分集示例按 `good-story-demo.md` 的“3、分集故事样例”及其 `Part 1` 至 `Part 5` 标记定位；这些 Part 当前是正文标记，并非独立 Markdown 标题，资产读取不能只识别 `##`。准备提示时由内容编辑核对功能摘要，选取前集结尾和紧邻下一集开头的真实文本，固定 Part 编号及所用片段；不在每个用户请求中增加摘要调用，也不默认附五集全文或叠加全部单篇示例。

五集样例可用于学习的章节功能如下，属于阅读分析，不是要求原作逐项满足的合同：

| 样例章节 | 叙事功能 | 可迁移到名著改写的方法 |
| --- | --- | --- |
| Part 1: The Strange Signal | 日常目标被异常打断，选择引出新的处境，结尾房门锁住 | 从原作的具体事件进入，让章末问题有已发生事件作为依据 |
| Part 2: The Locked Door | 开头立即尝试开门，转向通风口，行动进入下一场景 | 接住上集尚未解决的问题，交代新行动的原因与去向 |
| Part 3: The Vent Trap | 承接爬行位置，遇到摄像装置，尝试通过后触发警报与烟雾 | 场景变化有路径；原作存在行动后果时，把因果写在正文中 |
| Part 4: The Smoke Chamber | 处理烟雾危机、逃出管道，再面对办公室中的人物 | 先处理上集眼前危机，再进入下一任务，不跳过脱险过程 |
| Part 5: The Final Face-Off | 回应人物身份与事件缘由，结束中心冲突 | 终章回答核心疑问并留下情感回报，不为续读强加新谜题 |

样例中的“危险最终解释为演习”只能展示揭示信息的位置，不能当成可随意套用的因果解法；若原作没有对应设置与铺垫，不得添加这种反转。`protocol`、`ventilation shaft` 等词及复杂句式也不作为 Starter 语言标准。

提示按固定区块组合：

```text
任务与边界：忠实改编指定原作，只返回完整 title + chapters。
事实材料：指定原文范围 + 核对底稿。
阅读要求：实际档位 + 读者背景 + 已批准篇幅与章数。
写作指导：story-bible.md 中适用的语言、结构、内容原则。
叙事示例：单篇用选定示例；多章用分集功能摘要 + 一组相邻接缝原文 + 本次只学习的方法。
冲突说明：不移植示例情节；原作事实与目标档位优先；技巧不是逐项硬指标。
完整输出对象示例。
```

程序在创建任务时读取并固定这两份文件的版本或内容哈希、选定示例标题以及实际采用的提示片段，随任务断点保存；部署时打包参考资产，不能依赖服务运行目录恰好是仓库根目录。参考文件缺失或所选章节无法读取时在调用前报 `REFERENCE_UNAVAILABLE`，不静默退回旧提示。文档更新只影响新任务，续跑仍使用已固定的片段，避免评分依据中途改变。上述读取、选取和记录均不增加模型调用。

### 5.2 分级原则

复用现有 `readerStage` 标识和 `auto` 等级解析。明确区分：`readerStage` 决定语言难度，`examId` 提供读者背景与默认阅读负荷，作品单元决定可承载的情节范围。

| 等级组 | 主要改编策略 |
| --- | --- |
| `starter / stage1` | 少人物、少支线、具体动作、明确指代、必要因果直接说清楚 |
| `stage2 / stage3` | 保留主要场景与关键对话，适量心理和背景解释 |
| `stage4–stage6` | 保留更多氛围、次要关系与表达层次，仍控制阅读负担 |

以上是策略分组，不重新定义各档 CEFR 或词表；数值沿用既有档位资料，经样稿验证后单独调整名著策略。

低等级可以删支线、压缩重复事件、展开含糊指代，但不能改人物身份、动机、关键证据或结局。叙事连接句不得增加新的实质事件；不能为增加趣味强加笑点、搭档或新反派。

### 5.3 篇幅与章节

每个单元预先配置适用档位与章数，不向用户提供无法讲通的任意组合。请求超出批准组合时，在调用模型前返回可选组合。

MVP 一次生成完整短故事的所有章节，整体编辑结束后再开放第一章。章间悬念来自原作自然分段；终章解决本单元中心问题，不强制续集钩子。

参考分集样例安排章节时，每章都有实际推进，不只是把前一集摘要换一种说法。下一章开头承接上一章的地点、人物状态和待处理问题，允许一句必要复盘；新发现、人物认知改变和脱险过程以本次正文实际写出的事件为准。终章兑现原作在选定范围内的结果。以上由整篇改编和同一次编辑完成，不新增逐章 `handoff`、状态账本或接缝评审调用。

样例的五集功能可以在原作允许时合并为两至三章，或按原作自然场景组织为其他已批准章数；不能为了匹配五个 Part 扩写支线、复制危机或打乱原作关键事件顺序。章节与 Part 不要求一一对应，样例中的 `Theme` 标签只属于提示说明，不出现在读者正文中。

初期可复用现有阅读时长作为参考，但名著流程采用独立的篇幅配置，不直接调用带原创门禁的整套质量函数。总词数与单章长度有宽松范围；段落按叙事自然划分，不要求恰好四段。单元若无法装进模型经过实测的上下文或输出上限，准备阶段拆成有独立意义的范围，不在线静默截断原文。

篇幅控制以前置预算为正常路径：程序根据批准的总篇幅为每章分配目标区间和硬上限，并在改编提示中要求写在目标区间内、不要瞄准硬上限。例如三章总上限 1200 词时，每章目标为 300–350 词，硬上限为 400 词。核对底稿的必须保留项明显无法装入目标篇幅时，在模型调用前停止，要求缩小原作范围或改用更长规格。

整体编辑只返回需要修改的章节补丁；程序保留未列出的合格章节并合并补丁。增加解释时必须在同章删除等量或更多冗余文字。异常情况下只压缩超过本章硬上限的章节，输入该章正文及前后接缝，其他章节不交给模型重写；程序合并后同时检查总长度、压缩偏移和关键内容完整性。异常恢复最多三轮，仍不合格时保存完整稿并进入“需要修改后继续”，禁止截断正文、重新生成整篇或自动放宽批准上限。

### 5.4 输出结构

模型只负责文字：

```json
{
  "title": "英文改编标题",
  "chapters": [
    { "title": "英文章节标题", "paragraphs": ["英文正文"] }
  ]
}
```

章节编号、作品 ID、来源、版本、哈希由程序添加。不存在 `handoff`、`clueLedger`、`storyState`、逐段质量证据等创作附带任务。

## 6. 一次整体编辑检查

一次请求读取整篇改编、原文范围、核对底稿，以及本任务固定的 `story-bible.md` 原则和 `good-story-demo.md` 范例使用说明，处理：

- 是否遗漏关键因果或改变原作事实、关系与结局。
- 是否有跨章位置、物件、时间或人物认知矛盾。
- 是否写成事件梗概，缺少关键行动与选择的展开。
- 语言是否适合档位，难词是否过于集中，代词是否清楚。
- 是否存在不适龄内容，以及删减后是否仍能保持原意。

编辑用 `story-bible.md` 检查具体动作是否支撑因果、开头是否进入相关场景、结尾是否兑现本单元问题；用 `good-story-demo.md` 校准“清楚且有过程”的叙事标准。无需再次附示例全文。问题必须指向当前正文的实际缺口，不能因缺少某个范例场景、修辞或固定句数就判不合格；也不能把通用技巧扩展成新的线索、感官或章节升级门禁。

对多章改编，同一次编辑逐一阅读本稿实际相邻章节：上集眼前问题是否得到处理或明确保留、下集位置变化是否可理解、旧事实是否被误写为首次发现、终章是否解释并完成中心事件。参考分集样例的承接方法，不按样例的危机强度或反转方式打分；尤其不能因终章没有“新的危险”而判失败。

编辑不做四维打分，不与第一章比较分差，不要求忠实复现底稿里的某一句措辞。能在当前范围内解决的问题直接修改，返回整篇最终正文；无须改动也返回完整正文。

正常结果沿用 `title + chapters` 简单结构。无法在一次编辑内解决的源文本缺失、严重事实冲突或篇幅不相容，允许返回独立失败结果 `{"error":{"code":"SOURCE_CONFLICT","message":"具体原因"}}`；完整成功与失败形状都要在提示中展示，并采用对应本地联合校验，禁止混合返回半份正文。

编辑结果通过机械检查后保存为不可变正文版本。一次模型编辑不能保证没有语义错误，上线前需要样稿人工评估，上线后抽检；不将模型自检等同于人工质量保证。

### 6.1 名著流程的质量边界

| 检查 | 处理 |
| --- | --- |
| JSON 可解析、章节数量正确、标题与段落非空 | 必须满足 |
| 正文完整、无明显截断、符合已批准篇幅范围 | 必须满足 |
| 原作与底稿版本匹配 | 必须满足 |
| 故事忠实、因果完整、语言合适 | 一次编辑判断，结合人工样本评估 |
| 高频词覆盖率、平均句长、难词分布 | 诊断信息，提供给编辑，不单独循环重写 |
| 自然段数量、幽默、五感描写、每章新谜题 | 不设统一硬指标 |

初稿的非致命长度或语言问题交给这一次编辑统一处理；截断到无法理解或核心章节缺失属于输出不完整，走有界的输出恢复。编辑稿机械检查仍不合格时停止并保留两份稿件，不再启用原创候选循环。

## 7. 最后添加学习内容

只使用最终正文，题目不读取底稿、原作未改编部分或两份写作参考文档，避免答案依赖正文外知识。写作范例中的人物、事件和知识不能成为本故事的题目或选项依据。

1. 程序从每章实际词汇中选取少量学习词，复用词典与已有词形处理；选择数量按阅读档位配置。不把学习词反向塞回正文。
2. 每章生成两道四选一题，沿用现有产品的 detail 和 inference/cause_effect 类型。简单文本无法支持推断时允许第二道为 detail，不制造正文外推理。
3. 正确项索引、选项数量和差异、证据存在性由程序检查。可复用原文候选片段编号，编号对应的原文由程序回填；编号真实不代表答案逻辑自动正确。
4. 模型在同一次命题请求中核对答案是否唯一、证据是否支持选项。题目质量仍纳入人工抽检，不另起逐题打分工作流。

短故事优先一批生成全部章节题目，返回 `chapters: [{ chapterNumber, questions }]`，程序验证章节编号恰好覆盖目标集合。绑定 `textHash`，正文变化立即使旧题目失效。

题目失败只修复缺失或错误的章节题目，最多一次内容补做，不重写正文、不重新编辑。仍失败则保存正文，任务停在学习内容阶段供人工处理或明确手动重试。

复用查词、翻译、音频、阅读进度、答题和解锁能力；不为了兼容旧中间 Schema 编造 `qualityEvidence` 或 `storyState`。如果文章写入接口依赖这些字段，应抽出真正用于发布的最小字段映射。

## 8. 模型调用与 JSON 可靠性

核对底稿已经存在时，正常生成路径为三次模型请求：整篇改编一次、编辑一次、整批命题一次。学习词由程序选择。内容准备和人工核对不计入在线三次请求，恢复请求单独计数。

| 环节 | 正常调用 | 异常处理 |
| --- | --- | --- |
| 分级改编 | 1 | 输出结构损坏或截断最多恢复 1 次；不生成候选池 |
| 整体编辑 | 1 | 协议损坏最多恢复 1 次；不增加第二轮语义编辑 |
| 学习内容 | 1 | 只对失败题目补做 1 次；协议恢复与内容补做共用此额度 |

连接失败、429、服务过载等单独使用有上限的退避重试，每阶段最多两次传输重试；不消耗正文质量额度、不生成新候选。所有尝试次数持久化，重启不会清零。服务恢复后仍失败则停止等待明确重试。

实现要求：

- 每个提示末尾展示唯一完整根对象示例，不能只罗列字段；本地 Schema 与提示示例由同一处维护。
- 先实测选定服务端点的结构化输出能力。支持严格 JSON Schema 时使用最小 Schema；只支持 JSON object 时明确它不能保证业务字段完整，继续使用简单输出与本地校验。不能仅凭模型名称假设支持。
- 文本预算覆盖整篇章节、JSON 开销和服务实际思考行为，不复用单章约几百 Token 的限额。记录请求预算和 `finish_reason`，遇到 `length` 不把部分文本当完整正文。
- 原始响应及解析后的最佳候选在受控服务端保存，日志只显示阶段、错误路径、缺失字段与恢复次数，不记录密钥。
- 区分 `REFERENCE_UNAVAILABLE`、`OUTPUT_PARSE`、`OUTPUT_SCHEMA`、`OUTPUT_TRUNCATED`、`SOURCE_CONFLICT`、`CONTENT_REJECTED`、`LEARNING_INVALID`、`MODEL_UNAVAILABLE`，不再统一显示“结构不完整”。
- 恢复提示只带当前输出、完整目标形状与具体错误，不附上历史所有评分和废稿。

协议恢复不是一次新的评审：它只修复无法读取的输出。已经明确返回内容不合格的编辑结果不能借“结构恢复”重新请求直到通过。

## 9. 页面、API 与存储

### 9.1 用户流程

1. 选择“原创”或“名著改写”。原创表单和现有行为保留。
2. 名著改写展示已准备的作品及片段，提供简介、原作作者、改编范围、可选等级和预计阅读量。
3. 选择阅读档位，`auto` 展示实际解析后的等级；只展示资产支持的章数组合。
4. 不提供改变原作角色、结局、世界观的自由输入。若用户需要这些创作变化，使用原创入口。
5. 进度显示“读取原作资料 → 分级改编 → 编辑检查 → 生成学习内容 → 可以阅读”。已核对底稿不向用户伪装成每次都在现场核对。
6. 成功后展示“根据某作品某范围改编”、阅读等级、章节；沿用逐章解锁。

### 9.2 API

复用 `/custom-stories` 的创建、列表、详情和重试入口，在服务端使用按 `sourceMode` 区分的请求校验：

```json
{
  "sourceMode": "classic",
  "classicId": "作品标识",
  "unitId": "已核对片段标识",
  "readerStage": "stage1",
  "examId": "middle",
  "episodeCount": 3
}
```

示例是字段形状，不代表任意作品都支持该组合。服务端根据已发布资产验证并固定版本；客户端不能提交 `verified`、源文本路径或任意底稿版本绕过检查。

旧请求省略 `sourceMode` 时默认进入原创产品分支，保留当前定制入口使用 `favorite` 的内部映射，仍使用现有 `idea / characters / keywords / plotNotes` 等规则；不能为了 classic 把原创必填规则整体放宽。

提供只读名著目录接口，例如 `GET /classic-sources`，只返回可用作品单元与支持组合，不暴露内部文件路径。列表与详情响应增加 `sourceMode`、原作与片段显示信息、实际等级。

### 9.3 存储与断点

继续使用 `custom_story_requests` 和 `custom_story_logs`。建议增加：

- `source_mode`：旧用户定制请求回填 `favorite`，与当前入口一致；新请求保存实际选材模式，不能把迁移默认值当作历史事实推断。
- `classic_id / classic_unit_id`，原创为空。
- `source_version / base_version`，由服务端固定。
- `pipeline_version`，标识 `classic-v1` 或现有原创路径。

现有 `idea` 等非空字段可在 classic 请求中由程序填入作品说明，作为兼容展示字段，不承担原文输入职责。

复用 `checkpoint_json` 容器，但新增 classic 专用解析器；不得传入原创的 `parseStoryGenerationCheckpoint()`。最小断点包含类型/版本、源文本与底稿版本、两份写作参考文档的哈希和采用片段及示例标题、实际等级与篇幅配置、阶段、初稿/定稿、定稿哈希、题目结果、每阶段恢复计数及失败原因。

```text
source_ready → drafted → edited → learning_ready → published
```

任务执行状态继续沿用 queued / generating / failed / completed；失败不擦除最后一个成功阶段。手动重试从失败阶段续跑，同一正文的题目失败不得退回 drafted。修改源版本或阅读等级创建新生成版本，不混用原断点。

整篇编辑与学习内容完成后，在一个事务内写入全部章节并关联请求，再开放第一章。沿用后续章节解锁逻辑。写入以请求和章节编号保证幂等，崩溃恢复不能重复创建文章或投递。

第一版不建立跨用户全文缓存系统。内置内容可提前生成并沿用已有内容库分发；后续确有重复生成成本时，再按源版本、底稿版本、等级、篇幅和生成策略版本建立成品复用键。

## 10. 与原创隔离及代码落点

在生成入口按 `sourceMode` 分发一次，之后两条流程独立运行：

```text
sourceMode == classic → runClassicAdaptation()
其他模式              → 现有 runStoryGeneration()
```

用户队列入口和 CLI 都必须走同一分发规则，避免 CLI 的 classic 仍落回旧流程。可在已有 `generate-story-series` 对外入口处加入薄分发，保留原创实现原名与行为；不得让两个函数相互调用形成递归。

| 位置 | 计划改动 |
| --- | --- |
| `catalog.ts` | 接入已核对作品单元与支持档位，保留现有模式标识 |
| 新增 `story-generation/classic-pipeline.ts` | 资产加载、分级改编、一次编辑、学习内容与专用断点 |
| `story-bible.md`、`good-story-demo.md` 及构建资产打包 | 原文件作为写作依据；classic 读取适用片段并固定版本，不额外生成一套重复指导文档 |
| `generate-story-series` 对外入口、`cli.ts` | classic 路由、新增单元参数、版本选择和预检 |
| `server/src/app.ts`、客户端 API/表单类型 | 分类型参数校验、目录选择、来源与阶段展示 |
| `server/src/custom-story.ts` | 读取模式与版本，按流程解析断点、分发、处理重试与发布 |
| `server/src/database.ts` 对应迁移 | 增加请求来源字段及必要的发布幂等约束 |
| 现有词典、模型客户端、文章持久化 | 复用无原创策略依赖的底层能力 |

避免把 classic 的每个步骤塞回巨型 `pipeline.ts` 的条件分支；也不为两条流程建立通用插件引擎、工作流 DSL 或多代理编排。

现有 `completeQuestions` 等若绑定了原创季纲、评分和闭包状态，不直接复用整个函数。只提取所需的最小题目校验、词汇选择或文章写入能力，并保持原创调用和默认策略一致。

原创继续保留当前候选数量、评分阈值、线索校验、自动续跑额度和逐集发布。classic 不使用 `storyGenerationPolicy.review`、原创精英候选或按集质量重试。

## 11. 迁移与上线

1. 增加文档设计中的资产结构，准备少量真实原文与人工核对底稿。
2. 实现 classic 独立管线，先通过 CLI 跑通一个单元的两个等级；原创路径不变。
3. 完成题目、断点、错误分类、幂等发布，再接入 API 和前端入口。
4. 用固定样本验证质量与成本后开放已准备单元，逐步扩充内容库。

兼容策略：

- 历史记录缺少类型信息时默认保持原行为，不按标题推断它是名著；若其他元数据明确记录 classic，再做可审计迁移。
- 已发布旧名著文章继续可读，不自动重写。
- 旧 classic 在途任务固定旧流程版本直至结束；如需改走新流程，显式创建新任务并选择已核对单元，不能把旧季纲断点直接转换成核对底稿。
- 新 classic 请求只走新流程。新流程不可用或缺原文时给出明确原因，不静默退回模型凭书名创作。

## 12. 验证与验收

### 12.1 必要的程序验证

- classic 从 API 和 CLI 都进入独立管线；original / favorite 仍走原路径。
- 无原文、无核对记录、哈希变化或不支持的等级组合在调用前失败。
- 两份写作参考资产缺失时明确失败；改编请求实际包含适用原则与所选范例，多章默认包含分集功能摘要与真实相邻接缝，且能识别正文中的 Part 标记。编辑复用相同依据，命题请求不混入范例。续跑使用固定片段，文档更新不改变在途任务。
- 模型遗漏根包装、缺章节、返回截断时能准确分类并在额度内停止。
- 编辑失败不会进入评分/候选循环；题目失败只续跑学习内容。
- 重启不会重置恢复次数；同一请求恢复发布不会重复写入或重复投递。
- 题目证据属于最终正文，章节编号与题目数量正确；版本变化会使旧题目失效。
- 旧原创请求、断点解析和发布行为通过已有相关回归检查。

测试重点是分发、版本、恢复和发布边界，不为提示措辞写快照测试。

人工样稿核对还应覆盖：没有复制范例情节；没有为了倒计时、反转或危机升级改动原作；没有把范例词汇难度当成 Starter 标准；没有因自然段超过四句、必要复盘或终章无悬念而拒绝好稿。

多章样稿至少覆盖一份三章改编，确认五集样例没有强迫输出五章；检查实际每个接缝和最终收束，并确认正文中没有泄漏样例角色、情节、`Theme` 标签或演习反转。引用分集样例后，正常在线调用仍为改编、编辑、命题共三次。

### 12.2 内容与性能验证

先用 3 个适合的故事单元、每个两个阅读等级生成 6 份样稿，用人工阅读发现问题；这只是可行性验证，不足以宣称统计成功率。

评估记录：

| 指标 | 判断方式 |
| --- | --- |
| 原作忠实度 | 对照原文是否改变核心事实、动机或结局 |
| 故事完整性 | 是否保留目标、阻碍、选择、结果，是否退化为摘要 |
| 分级适配 | 对应水平读者是否能理解因果和指代，而非仅看词频 |
| 阅读吸引力 | 读者是否愿意继续，转折和结局是否有满足感 |
| 题目质量 | 答案唯一，证据充足，不依赖未改编内容 |
| 运行可靠性 | 首次完整成功率、失败阶段、恢复次数 |
| 成本与速度 | 分阶段耗时、总耗时、实际 Token 与每份可用成品成本 |

只有没有实质原作错误、因果断裂及明显不适配的样稿才进入内容库。后续扩大样本，与旧 classic 流程使用同样原作范围和档位对照，才能判断成功率和成本改善。

### 12.3 完成标准

- 产品两类入口清晰，名著改写来源可见。
- 每个开放单元都能追溯到实际原文与核对底稿。
- 同一底稿的不同等级独立生成，正常路径三次模型调用。
- 分级改编与一次编辑确实采用两份项目写作依据，能追溯版本；它们帮助表达，不覆盖原作事实、不增加硬性评分循环。
- 一次整体编辑后定稿；学习内容补做不改变正文。
- 无多候选融合、四维评分循环、模型生成的连续性账本。
- 失败可定位、可有界续跑，历史原创行为保持。

## 13. 参考依据

项目内直接采用的写作依据（具体适用边界见 5.1.1）：

- [story-bible.md](/Users/zhuangzhineng/Documents/ai_workspace/read-remember/story-bible.md)：语言、结构、内容与分集叙事原则。
- [good-story-demo.md](/Users/zhuangzhineng/Documents/ai_workspace/read-remember/good-story-demo.md)：《The Broken Robot》《The Shadow Map》单篇范例，以及 Part 1–5 分集故事样例；用于行动因果、相邻章节承接与终章收束。

分级改编采用已有成熟出版思路，但本项目生成质量需通过自身样本验证：

- [Penguin Readers Handbook](https://www.penguinreaders.co.uk/wp-content/uploads/2019/09/PR_ReadersHandbook_Main-1.pdf)：分级读物可以改编既有作品，需控制语言和信息呈现。
- [Oxford Bookworms](https://www.oup.com.au/elt/oxford-bookworms)：原创及改编文本按多个阅读等级提供。
- [Building Effective Agents](https://www.anthropic.com/engineering/building-effective-agents)：从简单流程开始，以实际评估决定是否增加复杂度。
